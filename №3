# Введення речення
z = input("Введіть речення: ")

# Розбиваємо речення на слова
words = z.split()

# Перетворюємо кожне слово: заміна 'a' на 'о'
new_words = [word.replace('a', 'о') for word in words]

# Виведення результату
print("Після заміни літери 'a' на 'о':", ' '.join(new_words))
